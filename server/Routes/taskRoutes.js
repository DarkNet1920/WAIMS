const express = require('express');
const taskRoutes = express.Router();
// const { AssignTask } = require('../Controllers/TaskController');

// taskRoutes.post('/assign-task', AssignTask);

module.exports = taskRoutes;